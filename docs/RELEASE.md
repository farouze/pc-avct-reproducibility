# Release and DOI record

Both authors approved the public repository and manuscript. Release `v38.0.0` published the primary V38 rerun; `v38.1.0` added the matched feature-family ablation; `v38.2.0` added the consolidated dataset-and-reference table; `v38.3.0` separated the submission manuscript and Supplementary Information while retaining a combined audit version; `v38.3.1` improves and shortens the abstract.

The GitHub validation workflow checks the public package without restricted data. The release contains no feature banks, participant predictions, or participant contrasts.

No software DOI has been issued. If an archival service is connected later, copy its issued DOI into `CITATION.cff`, the README, and the paper, and verify that it is distinct from the AuroraBP dataset DOI. Do not reserve space with a fabricated DOI or use the GitHub URL in a DOI field.

The suggested repository description is:

> Reproducibility code and provenance for the V38 PC-AVCT model-analysis rerun on AuroraBP, including paired participant inference, Holm correction, and a matched feature-family ablation.

The v38.3.1 release title is `PC-AVCT reproducibility release v38.3.1`.
